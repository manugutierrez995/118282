import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('discussion is honestly read-only and bookmark remains local',async()=>{const source=await readFile(new URL('../src/discussion/discussion.js',import.meta.url),'utf8');assert.match(source,/Public posting is read-only/);assert.match(source,/stored only in your selected local browser profile/i);assert.doesNotMatch(source,/textarea|Post anonymously|Google|rpc\(/)});
