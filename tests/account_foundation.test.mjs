import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {resolveRoute} from '../src/router/router.js';import {accountMenuMarkup} from '../src/account/navigation.js';
test('local profile and legacy routes resolve centrally',()=>{assert.deepEqual(resolveRoute({pathname:'/login',search:''}),{kind:'redirect',to:'/profiles?from=legacy-account'});assert.equal(resolveRoute({pathname:'/profiles',search:''}).kind,'profiles');assert.equal(resolveRoute({pathname:'/account/bookmarks',search:''}).kind,'account-bookmarks')});
test('account menu honestly presents local operations',()=>{const menu=accountMenuMarkup({displayName:'Reader',profileId:'local-id'});for(const label of ['Profile','Bookmarks','Settings','Export backup','Switch local profile','Delete local profile'])assert.match(menu,new RegExp(label));assert.match(menu,/Stored in this browser/)});
test('runtime has no Supabase imports, environment dependency, R2 credential or writes',async()=>{const files=['../src/main.js','../src/local-profile/store.js','../src/account/data.js','../src/discussion/service.js'];for(const file of files){const source=await readFile(new URL(file,import.meta.url),'utf8');assert.doesNotMatch(source,/supabase|VITE_SUPABASE|R2_SECRET|putObject|presign/i)}});
